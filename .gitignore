/target
.direnv/
.envrc.local
result
result-*

# Generated by git-hooks.nix (contains absolute /nix/store paths)
.pre-commit-config.yaml

# Claude Code internal state
.claude/scheduled_tasks.lock

# proptest failure-seed logs — transient developer-local artefacts, not tracked
# regressions (the test's own strategy + tolerances encode the invariants).
*.proptest-regressions
